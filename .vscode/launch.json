{
    // 使用 IntelliSense 了解相关属性。 
    // 悬停以查看现有属性的描述。
    // 欲了解更多信息，请访问: https://go.microsoft.com/fwlink/?linkid=830387
    "version": "0.2.0",
    "configurations": [
        
        
        {
            "name": "直接启动 PokerGame",
            "type": "node-terminal",
            "request": "launch",
            "command": "$env:PATH = 'D:/QT/6.8.3/mingw_64/bin;' + $env:PATH; ${workspaceFolder}/build/PokerGame.exe",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "CMake: build step"
        }
    ]
}
